services:
  - type: web
    name: traveller-saanga
    runtime: node
    plan: free
    buildCommand: npm ci && npm run build && cd server && npm ci --only=production
    startCommand: cd server && node index.js
    healthCheckPath: /health
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
